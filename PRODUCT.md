# Product

<!-- impeccable:product-schema 1 -->

## Platform

ios

## Users

미술관·전시장을 방문 중이거나 방문을 계획 중인 한국 관람객. 전시장 안에서는 눈앞의 작품에 대해 즉석으로 맥락을 얻고 싶어 하고, 방문 전후로는 근처에서 볼 만한 전시를 찾고 다녀온 경험을 기록하고 싶어 한다.

## Product Purpose

관람객이 작품을 촬영하거나 제목/작가를 직접 입력하면 AI가 한국어 설명을 생성해 타이프라이터 애니메이션으로 보여주고 TTS로 낭독해 주는 "개인 도슨트" 기능과, 지도·검색·북마크로 전시를 발견하고 방문을 다이어리로 남기는 "전시 탐색" 기능을 한 앱에서 제공한다. 성공은 관람객이 작품 앞에서 막힘없이 맥락을 얻고, 다음에 갈 전시를 쉽게 찾고, 다녀온 기록을 자연스럽게 남기는 것이다.

## Positioning

AI 도슨트(사진/수동 입력 → AI 설명 생성 + TTS 낭독 + AI와의 채팅)와 전시 탐색(지도 마커, 검색, 북마크, 방문 다이어리)을 하나의 앱에서 결합한다. 오디오가이드 앱이나 전시 정보 앱 단독으로는 제공하지 못하는 조합이 핵심 메커니즘이다.

## Operating Context

관람객은 세 시점에서 앱을 쓴다: (1) 전시장 안에서 — 작품 촬영 또는 수동 입력 → AI 설명 스트리밍 생성 → 타이프라이터 표시 → TTS 재생 → AI 도슨트 채팅. (2) 이동 중 — 지도 탭에서 미술관/전시 마커를 탐색하고, 날짜·영업중·전시진행중 필터를 걸고, 바텀시트로 장소별 진행 중/예정 전시를 확인. (3) 방문 이후 — 아카이브 탭에서 북마크한 전시를 다시 보고 다이어리에 방문 기록을 남긴다. 전시장 안은 네트워크가 약할 수 있는 환경으로 취급한다.

## Capabilities and Constraints

- 클라이언트는 외부 AI(Anthropic)·TTS(ElevenLabs) API를 직접 호출하지 않고 항상 Supabase Edge Functions를 경유한다.
- 전시·미술관 데이터는 소스가 섞여 있다: 정적 큐레이션 데이터(`src/data/exhibitions.ts`), 문화포털 공공 API, KCISA 캐시(Supabase `kcisa_exhibitions`), 미술관 목록 공공 API, 수동 큐레이션 테이블(`manual_exhibitions`). 소스별 커버리지 공백이 실재하며, 없는 데이터는 지어내지 않고 "정보 없음"류 문구로 자연스럽게 드러낸다.
- 전 화면 한국어 우선. 터치 가능한 요소는 accessibilityLabel/accessibilityRole 필수(컴포넌트 컨벤션에 이미 명시).
- 그늘/경사 기반 길찾기는 구상 단계이며 별도 SPEC으로 분리된 미구현 기능 — 현재 범위 아님.

## Brand Commitments

웜뉴트럴 팔레트(`#F8F6F2`/`#F2EFE9` 배경, `#1C1917` 잉크), Pretendard(본문)/Hahmlet(타이틀) 폰트, 둥근 모서리 카드 기반 컴포넌트. 프로젝트 CLAUDE.md에 이미 확정된 기존 디자인 언어.

## Evidence on Hand

정적 데모 전시 6건(모네·이중섭·달리·김환기·KAWS·반고흐)과 예시 이미지 에셋이 저장소에 존재. 문화포털·KCISA·미술관목록 공공 데이터 API 연동은 실제 호출로 확인됨. 사용자 후기, 가격 정책, 실제 방문자 통계 등은 없음 — 향후 작업에서 지어내면 안 된다.

## Product Principles

1. 실시간 API 결과와 정적 큐레이션 데이터를 섞어 보여줄 때도 어떤 게 데모이고 어떤 게 실시간 데이터인지 코드에서 구분 가능하게 유지한다.
2. 데이터 공백은 숨기지 않고 정직하게 드러낸다 ("진행 중인 전시가 없어요" 등).
3. 전시장 안의 약한 네트워크를 염두에 두고 API 실패는 조용히 폴백 처리한다.
4. 한국어 사용자 기준의 타이포그래피와 톤을 유지한다.
