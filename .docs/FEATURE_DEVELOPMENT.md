# FEATURE_DEVELOPMENT.md

신규 기능 아이디어 백로그 정본. John(기획)·Sam(디자인) 브레인스토밍 세션(2026-09-11)에서 나온 아이디어를 사용자 검토 결과로 필터링해 유지한다.

**사용 규칙**: 이 세션에서 새 기능을 논의할 때마다 이 문서를 먼저 참고한다. 상태가 바뀌면(진행 시작/보류 해제/기각) 이 문서를 갱신한다.

기준 시점: 2026-09-11

---

## 1. 진행 대상 (우선순위별)

### 1순위 — 기존 인프라 재사용, 비용 낮음

| 기능 | 요약 | 근거 |
|---|---|---|
| 감상 에세이 생성 (확정 큐 통합) | **rev2 구현 완료(2026-09-12)**: 한줄평+별도 감상 생성 단계를 "감상평" 단일 입력칸으로 통합. 별점 → 감상평(직접 쓰기/생성하기) → 서명. G6 handoff 완료, 실기 확인 대기(`06-handoff-to-user.md` 참고). 시뮬레이터에 앱 바이너리가 없어 스트리밍/토글 실기는 미확인 | rev2. Alex Design QA Pass @ iteration 2(4.71/5), Taylor QA Pass(조건부), tsc 0 errors, jest 94/94 |
| 오프라인 다운로드 | **범위 변경(2026-09-11)**: 재생목록 전체 → **북마크(하트)한 해설만** 오프라인 저장. 조회는 기존 `app/settings/bookmark/audio.tsx`를 확장(신규 화면 불필요). 사유: "재생목록 전체 다운로드"는 (a) 관람 종료 후 조회 화면이 없었고 (b) 다이어리/스탬프북과 달리 오디오 재청취 수요 자체가 불확실해, 사용자가 명시적으로 의도를 표시한 북마크 항목으로 축소. AC-1~4(재생목록 전체 대상)는 QA Pass까지 갔으나 폐기, 인프라(`offlineAudio.ts`/`offlineDownloadStore.ts`/`resolveAudioUri`/`DownloadStatusBadge`)는 재사용 예정 | 통신 불안정 전시장 대응. 축소로 공수 감소 + 기존 북마크 화면 재사용 |

### 2순위 — 신규 인프라 소량 필요

사용자 재검토 (2026-09-12). 스탬프를 키워 공유하는 방향은 접었다.

#### 다음으로 손댈 후보

| 기능 | 무엇을 만드는가 | 메모 |
|---|---|---|
| 위치기반 알림 | 근처 진행 중 전시를 푸시. 주간 배치가 아니라 **낮에 하루 1~2회**. 알림 탭에 남긴다. | 사용자 OK. **원격 푸시(APNs/Expo Push)는 유료 Apple Developer가 필요.** 이 레포는 Personal Team 때문에 `expo-notifications`를 exclude·주석 처리해 둠. 로컬 알림+낮 시간 위치 체크는 이론상 유료 없이 시뮬/본인 폰 테스트 가능하나, 모듈을 다시 켜야 함. 지금은 개발자 계정 산 뒤에 스파이크. |
| 다이어리에서 해설 바로 재생 | 날짜 상세 티켓 뒷면 프로그램을 탭하면 북마크 오디오와 같이 시트에서 재생. 그날 `historyStore` 해설 텍스트를 매칭. | 구현됨. 온보딩 개편이 다음 트랙. |
| 온보딩 개편 (관람 초대권 + 첫 해설) | 티켓 스택 소개 → 샘플 청취 2클립 → 초대권 3~5장 → 입장. taxonomy 장르만 persist, 추천 빈 `.in` 폴백. 설정 취향은 초대권만. | **G6 실기 확인 대기 (2026-09-13).** `06-handoff-to-user.md`. tsc 0, jest 107/107, Alex Pass @1. |

#### 디자인 레퍼런스 후 재논의

| 기능 | 무엇을 만드는가 | 메모 |
|---|---|---|
| 관람 기록 공유 (티켓 카드) | 공유한다면 스탬프가 아니라 **티켓/영수증 카드**. `VisitTicket*` + `useShareExhibition`. | 상으로 뒀으나 디자인이 본질. 지금 스탬프(`VisitStamp`)는 “관람 완료” 도장 1종이라, 스탬프를 예쁘게 만들어 공유·수집을 확장하는 건 기능이 아니라 디자인 시스템 작업이다. 레퍼런스 보기 전까지 구현 착수하지 않음. |

### 3순위 — 검증 필요

| 기능 | 요약 | 근거 |
|---|---|---|
| 작품 자동 인식(캡션 없이) | `extract-text`가 텍스트만 추출 중인데, 이미지 자체로 작품/작가 추정까지 확장 | 프롬프트 확장 수준이나 정확도 검증 필요 |
| 다국어 지원 | 해설/TTS가 한국어 전제 — 외국인 관광객 대응 | 리소스 큼, 별도 검토 |

### 안전망 (필수 기능 — 별도 트랙)

| 기능 | 요약 |
|---|---|
| 리뷰/별점 | 전시 상세에 사용자 리뷰/평점 없음 — 커뮤니티성 부재 |
| 잠금화면/백그라운드 재생 컨트롤 | 오디오 앱 기본기 — `expo-audio` 잠금화면 제어 여부 확인 필요 |
| 접근성 강화 | 폰트 크기 설정은 있으나 고대비 모드, 자막(해설 텍스트 동시 노출) 등 미흡 |

---

## 2. 보류 / 기각 (사용자 검토 결과, 2026-09-11)

| 기능 | 판정 | 사유 |
|---|---|---|
| 동선 최적화 재생목록 | **불가** | 전시장 내부 작품 위치·경로 데이터를 확보할 방법이 없음 |
| 하루 코스 추천 | **보류 — 중복 확인 필요** | 전시 상세 `route` 화면에 이미 여러 전시관 묶는 기능이 있을 가능성 — 재확인 후 재논의 |
| 전시 전체 큐레이터 챗봇 | **보류 — 1번과 동일 맥락** | 동선 최적화와 같은 이유(전시장 내부 구조 데이터 부재)로 막힐 가능성 — 재검토 필요 |
| 음성 클로닝 | **불가** | 유명인/연예인 목소리 클로닝은 저작권 문제. 가족 목소리 클로닝(본인 동의 기반)은 이론상 가능하나 우선순위에서 제외 |
| 예매 연동/딥링크 | **보류** | 실제 티켓 예매가 필요한 전시 비중이 낮음. 현재도 CTA "예매하기" 버튼은 존재 — 추가 개발 불필요 판단 |
| 연간 미술관 리포트 | **보류** | 연 5회 관람이면 랩드가 아니라 목록이다. 데이터 밀도가 생길 때까지 내리지 않음 (2026-09-12) |
| 취향 재학습 | **보류 — 온보딩 개편으로 흡수** | 추천 주기 갱신이 아니라 온보딩 자체를 갈아엎을지 고민 중. 추후 온보딩 단독 스펙 (2026-09-12) |
| 스탬프 기능 확장 | **접음** | 스탬프가 예뻐야 공유한다는 전제는 스탬프 디자인 시스템이 됨. 기능으로 키우지 않음 (2026-09-12) |

---

## 3. 이력

- 2026-09-13: 온보딩 개편(관람 초대권 + 첫 해설) 구현·G6 핸드오프. 위치 알림은 유료 개발자/푸시 자격증명 이슈로 후순위
- 2026-09-12: 다음 작업 확정 — 다이어리 바로 재생 구현 후 온보딩 개편 스펙. 위치 알림은 유료 개발자/푸시 자격증명 이슈로 후순위
- 2026-09-12: 2순위 재검토 — 위치알림·다이어리 재생을 다음 후보, 공유는 티켓 카드로 축소 후 디자인 대기, 연간리포트·취향재학습·스탬프 확장은 보류/접음
- 2026-09-12: 2순위 5개를 상/중/하로 1차 분류 (이후 같은 날 재검토로 대체)
- 2026-09-12: 감상 에세이 생성 rev2 — 한줄평/감상문 통합 입력칸 구현, G6 handoff
- 2026-09-11: John·Sam 브레인스토밍 초안 생성 → 사용자 검토로 5개 항목 보류/기각, 1순위 3개 확정
